[project]
name = "program-mcp-server"
version = "0.1.0"
description = "FastMCP server for Coca-Cola program activation"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=2.13.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
